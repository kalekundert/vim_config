" Disable the built-in indent expression because doesn't allow comments to be 
" indented (even though the code looks to me like it should).  Also, I don't 
" really like indenting LaTeX markup that much anyway.
set indentexpr=
set autoindent

